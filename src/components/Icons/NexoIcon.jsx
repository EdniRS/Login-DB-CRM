import React from 'react';
import { GamepadDirectional } from 'lucide-react';

// Le pasamos props como className para poder darle estilos desde CSS
const NexoIcon = ({ className }) => {
    return (
        <GamepadDirectional />
    );
};

export default NexoIcon;